#ifndef ALGO_MACROS_H
#define ALGO_MACROS_H

#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define CLAMP(val, min_val, max_val) ((val) < (min_val) ? (min_val) : ((val) > (max_val) ? (max_val) : (val)))

#endif