#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    int size = 100;
    char str[100];
    char sub[100];

    printf("Main string: ");
    scanf("%s", &str);
    printf("Substring: ");
    scanf("%s", &sub);


    while (1) {
        char *subs = strstr(str, sub);
        if (subs != NULL) {
            char str1[100];
            char mid[100];
            char str2[100];
            for (int i=0; i<str-subs;i++) {
                str1[i]=str[i];
                }
            for (int i=str-subs; i<str-subs+strlen(sub);i++) {
                mid[i]=str[i];
                }
            for (int i=str-subs+strlen(sub); str[i]!='/0';i++) {
                str2[i]=str[i];
                }
                strcat(str1,str2);
            strcpy(str1,str);
            }
        else break;   
        }

    printf("%s", str);
    return 0;
}